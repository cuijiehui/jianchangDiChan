<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:focusable="true"
    android:focusableInTouchMode="true">
<include layout="@layout/include_top_layout"/>
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="95dp"
        android:focusable="true"
        android:focusableInTouchMode="true">
        <EditText
            android:id="@+id/et_feedback_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="请输入你的反馈意见（10-140字）"
            android:textColorHint="@color/main_flash_sale_text"
            android:layout_marginLeft="16dp"
            android:layout_marginRight="16dp"
            android:background="@drawable/et_bg_shape"/>
    </ScrollView>

    <EditText
        android:id="@+id/et_feedback_phone"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:hint="请输入您的联系方式"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:textColorHint="@color/main_flash_sale_text"
        android:background="@drawable/et_bg_shape"/>
    <Button
        android:id="@+id/bt_feedback_opinion"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:layout_marginLeft="14dp"
        android:layout_marginRight="14dp"
        android:text="提交"
        android:layout_marginTop="10dp"
        android:textColor="@color/white"
        android:background="@drawable/bt_fullpage_pass_shape"/>
</LinearLayout>
